services:
  meeting-app:
    build: .
    container_name: meeting-app
    ports:
      - "3001:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - DATA_DIR=/app/data
    restart: unless-stopped
